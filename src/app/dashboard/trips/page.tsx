import DashboardPage from "@/components/shared/dashboard-page";

export default function TripsPage() {
  return (
    <DashboardPage
      title="Trips"
      description="Manage coffee collection trips, drivers, field weights and factory arrival information."
    />
  );
}
