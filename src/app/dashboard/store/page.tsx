import DashboardPage from "@/components/shared/dashboard-page";

export default function StorePage() {
  return (
    <DashboardPage
      title="Store"
      description="Manage received coffee, storage locations, lots, quantities and stock movements."
    />
  );
}
