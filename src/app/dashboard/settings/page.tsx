import DashboardPage from "@/components/shared/dashboard-page";

export default function SettingsPage() {
  return (
    <DashboardPage
      title="Settings"
      description="Configure system preferences, coffee settings and administrative options."
    />
  );
}
